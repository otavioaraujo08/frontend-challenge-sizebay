import { Modal } from '@components/modal/index.tsx';
import './app.ts';
import { Container } from './app.ts';

export const App = () => {
  return (
    <Container>
      <Modal />
    </Container>
  );
};
